# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Virtual environments
venv/
.venv/
env/

# Django
# The database is rebuilt with "manage.py migrate" + "manage.py seed",
# so it is deliberately NOT committed: a stale database would fight the
# pinned note ids that the access-control demos rely on.
db.sqlite3
db.sqlite3-journal
*.log
/staticfiles/
/media/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
